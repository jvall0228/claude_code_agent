{
  "mcpServers": {
    "clawdkit-heartbeat": {
      "command": "bash",
      "args": ["-c", "cd {{HEARTBEAT_MCP_PATH}} && bun server.ts"],
      "env": {
        "CLAWDKIT_AGENT_NAME": "{{AGENT_NAME}}"
      }
    }
  }
}
